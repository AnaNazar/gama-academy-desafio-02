import styled from 'styled-components';

export const Container = styled.div`
    background: #d9e4ec;
    width: 100%;
    min-height: 120px;
    padding: 20px 0;
    display: flex;

    h1 {
        font-size: 26px;
        color: #1e3d58;
        text-transform: uppercase;
    }
`;
